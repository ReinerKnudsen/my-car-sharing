name: Changelog Automation

on:
  pull_request:
    types: [closed]
    branches: [main]

jobs:
  update-changelog:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run changelog automation
        id: changelog
        run: node scripts/update-changelog.js
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Commit and push changes
        if: steps.changelog.outputs.version_updated == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add package.json changelog.md
          git commit -m "chore: release version ${{ steps.changelog.outputs.new_version }}"
          git push

      - name: Create git tag
        if: steps.changelog.outputs.version_updated == 'true'
        run: |
          git tag v${{ steps.changelog.outputs.new_version }}
          git push origin v${{ steps.changelog.outputs.new_version }}

      - name: Create GitHub release
        if: steps.changelog.outputs.version_updated == 'true'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ steps.changelog.outputs.new_version }}
          name: Release v${{ steps.changelog.outputs.new_version }}
          body_path: ${{ steps.changelog.outputs.release_notes_file }}
          draft: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Trigger Netlify Deploy
        if: steps.changelog.outputs.version_updated == 'true'
        run: |
          echo "🚀 Triggering Netlify build with version ${{ steps.changelog.outputs.new_version }}"
          curl -X POST -d '{}' ${{ secrets.NETLIFY_BUILD_HOOK }}
          echo "✅ Netlify build triggered"

      - name: Sync changelog back to dev
        if: steps.changelog.outputs.version_updated == 'true'
        run: |
          echo "📝 Syncing updated changelog back to dev branch"
          git fetch origin dev
          git checkout dev
          git merge main --no-edit
          git push origin dev
          echo "✅ Dev branch updated with new changelog"
